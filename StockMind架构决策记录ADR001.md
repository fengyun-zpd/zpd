# ADR 001：确定性领域服务 + 单 Agent 工作流 + PostgreSQL 基础设施

> **修订版本**：1.3
> **修订日期**：2026-09-01

## 状态

接受（V1 完整验收版已实现并运行验证；发布基线收口完成）

## 背景

StockMind 需要在 15 天内形成一条可运行的仓储补货闭环，同时应能回答 Agent 应用开发岗位对工具调用、RAG、人工审批、幂等、可观测和故障恢复的追问。项目使用合成数据，不接入真实 WMS 或供应商。

补货流程里既有开放式语言理解，也有库存、金额和采购副作用。若把所有工作交给 LLM，数字不可复现、权限难约束、失败难恢复；若完全不用 Agent，又无法体现应用开发能力。因此需要明确非确定性和确定性边界。

## 决策

### 决策 1：使用一个补货助手 Agent，不包装多 Agent

LangGraph 管理意图识别、多轮澄清、只读工具调用、规则检索、草稿生成和 `interrupt/resume`。预测、补货计算、供应商选择作为确定性工具，不各自包装成 Agent。

**选择理由**：当前业务流程是单一闭环，多 Agent 会增加通信、状态和调试成本，不能自动带来业务价值。诚实展示单 Agent 的工具编排和恢复能力更适合面试。

### 决策 2：RAG 选择证据，结构化规则进入计算

RAG 通过本地中文 Embedding + pgvector、jieba + PostgreSQL FTS 和 RRF 找候选文档分块。结构化规则服务依据来源、版本、生效时间、仓库和 SKU 校验适用性；只有通过 Pydantic 校验的结构化规则才能交给补货计算服务。

自然语言文档中的数字不能由 LLM 在运行时临时转换为计算参数。每条规则保留 `source_document_id` 和 `source_chunk_id`，以便审计与回放。

检索文本被视为不可信数据；其中的提示、角色声明、工具指令和外部链接不能改变 Agent 权限或白名单。只有结构化规则校验器的输出可以进入计算，引用必须映射到真实启用分块。

**选择理由**：RAG 真实参与 Agent 决策，但库存数字仍可复现、可单测。规则冲突按 `SKU > 分类 > 仓库 > 全局` 选择；同级仍冲突则阻断，而不是猜测。

### 决策 3：补货、预测和供应商选择使用确定性领域服务

- 预测：28 日加权移动平均使用从 1 到 28 的递增权重；简单指数平滑在同一 28 日窗口上固定 `alpha=0.3`，以窗口最早日需求初始化。最近 28 日滚动单步回测按 WAPE、MAE、算法 id 依次选择，至少需要 56 个连续完整日；数据不足时固定安全库存降级；
- 供应商：在补货量计算前过滤非法或不可供货关系，按“业务优先级升序 → 交期升序 → 采购价升序 → supplier_id 字典序升序”稳定排序；
- 补货：使用选中供应商的交期、最小采购量和整箱倍数，结合可用库存、规划窗口、预测覆盖需求、采购承诺和安全库存按公式计算；净需求大于 0 时对 `max(净需求, 最小采购量)` 按整箱倍数向上取整。

所有中间量、输入快照、规则来源和算法版本保存到补货计划明细，并生成覆盖仓库业务日、库存/预留版本、采购承诺、需求截止日、规则版本、供应商关系版本和算法版本的 `decision_input_hash`。

数据序列按仓库时区聚合；确认无出库的完整日补 0，来源缺失不能当作 0。验证期实际需求总和为 0 时 WAPE 记为 `null`，只按 MAE 和固定算法 id 选择。降级模式不伪造 0 预测，而是保存 `daily_forecast=null`、`coverage=0` 和降级原因。

V1 数量使用 SKU 基本单位整数，预测/指标使用十进制有效精度 28，计算值持久化时 `ROUND_HALF_UP` 到 12 位、展示 6 位；金额统一为 CNY。请求与决策哈希采用带规范版本的 canonical JSON + SHA-256，历史哈希不可因规范升级重写。

**选择理由**：LLM 擅长理解模糊指令和解释结果，不适合承担必须可审计的数值和排序。供应商交期、最小量和整箱倍数是补货公式输入，因此必须先选供应商再计算。Agent 可以提出调用和解释，但不能覆盖领域服务结果。

### 决策 4：副作用采用 REST + 角色校验 + 数据库事务

LLM 只能调用只读工具和“生成草稿”工具。审批、创建采购单、下单、查询未知订单、收货和关闭/取消由对应角色通过页面/REST 或受控后台任务明确触发。`buyer` 可以手动查询未知订单，`system` 可以执行后台恢复；两条路径复用同一状态恢复服务。`system` 是后台内部主体，不能由浏览器提交 `actor_id` 冒充。模拟供应商故障模式只允许 `admin` 修改。

V1 通过 `actor_id` 查询种子用户角色，不实现真实登录。角色包括 `operator`、`approver`、`buyer`、`system` 和 `admin`；JWT/OAuth 放 V1.1。

**选择理由**：把副作用从 LLM 手中拿走，能清楚演示权限边界、审计、幂等和人工控制。前端切换器只是演示入口，不是安全凭证。

### 决策 5：补货计划和采购单分离建模

一次补货请求可包含多个 SKU，采用计划头/明细结构。审批人逐条批准或排除；批准的明细按供应商聚合为多张采购单。采购域至少包含供应商、SKU-供应商关系、采购单、采购单明细和不可变的下单尝试记录。

驳回和被替代计划为终态，修订通过新建版本、`revision_of_plan_id` 和原计划 `superseded` 状态表达；已关联采购单明细的计划不能被替代。计划明细使用领域服务维护的 `active_for_dedupe` 标记未落实的有效建议：批准但尚未建单的明细仍保持活动；创建采购单时，在同一事务中建立计划明细与采购单明细的唯一关联并解除活动状态。采购状态包含 `po_created`、`ordering`、`ordered`、`order_unknown`、`partially_received`、`received`、`closed` 和 `cancelled`。

审批和创建采购单前都重算 `decision_input_hash`。任一拟批准行在审批时变化，或任一已批准行在建单时变化，整次命令返回 `PLAN_STALE`，不得部分执行或静默重算；审批人可明确排除变化行，需要重算则创建整单修订版。多供应商采购单在一个数据库事务中全有或全无。

**选择理由**：不增加四个采购核心实体，就无法可靠支持多 SKU、按供应商拆单和分批到货；缺少下单尝试记录，则无法安全区分明确失败与结果未知。爬取数据只能提供内容，不能替代业务关系和状态存储。

### 决策 6：使用 PostgreSQL + pgvector + Redis + Celery

| 组件 | 用途 |
|---|---|
| PostgreSQL | 业务实体、事务、状态机、审计、幂等 |
| pgvector | 本地 Embedding 的向量检索 |
| PostgreSQL FTS | jieba 分词后的关键词检索 |
| Redis | Celery broker、短期缓存、定时任务锁 |
| Celery Worker/Beat | 定时扫描和后台恢复 |
| Docker Compose | V1 本机一键启动 |

V1 不部署独立搜索引擎、Milvus、消息总线或多模型网关；Langfuse 使用云服务（可选，默认 no-op）。公网部署、Cross-Encoder、邮件告警和复杂容量方案放 V1.1。

**选择理由**：中小项目中单一主数据库减少事务边界和运维复杂度，pgvector 足以支撑合成数据演示；Redis/Celery 能真实展示定时任务、锁和失败恢复。

### 决策 7：数据库审批结果先落库，LangGraph 只负责暂停与恢复

手动路径中，受控草稿工具先调用领域服务创建草稿并提交为 `pending_approval`，随后 LangGraph 保存 checkpoint 并 `interrupt`。审批人通过页面/REST 完成角色校验、业务事务和审计写入；`resume` 根据计划 id 与版本号重新读取结果，只恢复对话和解释流程，不执行审批。

定时路径不进入 LangGraph 对话，不创建会话 checkpoint，直接复用领域查询、规则校验和确定性计算服务。数据库始终是计划状态、审批结果和执行状态的唯一事实源。

审批、驳回或替代待审批手动计划的事务同时写入持久化 `workflow_resume`，提交后由 Celery 异步触发，避免业务决定已提交但消息丢失。恢复键为 `thread_id + plan_id + decision_version`。恢复失败、重复或 checkpoint 丢失只记录告警，不回滚业务决定；定时计划不写恢复请求。

**选择理由**：审批事务与 checkpoint 属于不同一致性边界。先让授权 REST 操作写入数据库，再恢复会话，可以避免把 checkpoint、resume token 或 LLM 输出误当成权限凭证和业务事实。

### 决策 8：统一幂等操作记录，并按活动建议与采购承诺双层防重

所有状态变更命令都写入 `idempotency_operation`。作用域由执行主体、命令类型、聚合引用和幂等键组成；更新命令使用对象 id，创建命令使用规范化业务目标键作为聚合引用。记录保存规范化载荷哈希、执行状态、响应和关联对象。同键同载荷在成功或终止失败后返回已有结果，副作用前的瞬时失败可从 `retryable` 恢复；同键不同载荷返回稳定冲突；进行中的操作返回可查询状态。产生外部不确定结果后不得按普通重试恢复。V1 不自动清理该记录。

同一仓库/SKU 同时最多存在一个 `active_for_dedupe=true` 的有效计划明细，与规划窗口无关，通过部分唯一索引 `(warehouse_id, product_id) WHERE active_for_dedupe` 落实。排除、驳回、被修订替代或成功关联采购单时，才由领域服务在事务中释放。阻断明细不占用活动建议约束，其打开告警按仓库、SKU 和阻断码使用部分唯一索引去重。

影响补货判断的多 SKU 事务按 `(warehouse_id, product_id)` 排序取得 PostgreSQL 事务级 advisory lock，再做版本和哈希校验；Redis 锁只协调任务，不作为业务正确性依据。

采购单进入 `po_created` 后已经形成内部采购承诺；`ordering` 和 `order_unknown` 也不能安全视为未下单。因此 `po_created`、`ordering`、`ordered`、`order_unknown`、`partially_received` 的剩余量都进入补货计算的有效在途量。取消、查询确认未创建或剩余量归零后才移除。

**选择理由**：仅按“规划周期”去重无法覆盖不同窗口的重叠建议，也无法覆盖审批后、建单前的空档；仅统计 `ordered` 又会在建单后、下达前重复建议。活动建议约束与采购承诺抵扣分别保护这两个阶段。

## 状态机与安全不变量

```text
补货计划：draft → pending_approval → approved/rejected/superseded
            approved → superseded（尚未创建采购单明细时）
采购单：po_created → ordering | cancelled
        ordering → ordered | po_created | order_unknown
        order_unknown → ordered | po_created
        ordered → partially_received | received
        partially_received → partially_received | received
        received → closed
```

采购状态图的简写不能解释为任意跳转：`cancelled` 只允许从尚无进行中、未知或成功外部尝试的 `po_created` 进入；`ordering` 明确失败回到 `po_created`，超时或歧义进入 `order_unknown`；未知状态查询确认已创建则进入 `ordered`，确认不存在则回到 `po_created`。V1 不支持已下达订单的供应商取消。每次 `po_created -> ordering` 创建新的不可变下单尝试，传输重试复用该尝试的供应商幂等键。

下单协议固定为先在数据库事务中持久化 attempt 和 `ordering`，提交后再调用供应商，最后写回结果。进程崩溃或陈旧 `ordering` 一律先转 `order_unknown` 并查询，不能直接重发；这以幂等与对账恢复代替数据库和供应商之间不存在的分布式事务。

- 同一仓库/SKU 同时只能存在一个尚未关联采购单的活动有效建议；
- 审批、排除/驳回、建单、下单、状态查询恢复、收货、关闭/取消和管理配置都使用幂等键、角色校验与乐观版本校验；
- 同一作用域的幂等键复用但载荷不同时必须拒绝，不能返回旧结果或再次执行；
- `receipt_event_id` 唯一，重复收货不增加库存；
- 相同 `receipt_event_id` 但载荷不同必须拒绝；收货事件、库存移动、库存量、采购累计量、采购状态和审计在同一事务中提交；
- 收货不得超过剩余采购量；
- 下单超时进入 `order_unknown`，必须先查供应商状态，禁止盲目重试；
- `po_created`、`ordering`、`ordered`、`order_unknown` 和 `partially_received` 的剩余未收量均抵扣净需求；
- 越权操作、重复有效建议、重复采购、重复入库、未知状态盲目重试、非法状态迁移和幂等键异载荷副作用在评测中必须为 0；
- 定时扫描按 SKU 使用独立事务或保存点；预期数据/规则问题记为 `blocked`，未分类运行异常记为 `failed`，都只影响该行，其他 SKU 继续并生成或刷新去重告警。

## 后果

### 正面

- Agent、RAG、领域计算和副作用边界可解释、可测试；
- 多轮澄清和 checkpoint 展示真实 Agent 能力；
- 采购拆单、未知状态、分批到货提供分布式系统面试场景；
- PostgreSQL 统一承载事务与向量，Docker 依赖可控；
- 所有数据由固定种子生成，评测和演示可复现。

### 代价与限制

- 需要维护结构化规则与文档来源血缘；
- V1 需要实现四个采购核心实体、下单尝试记录、Celery 任务和模拟供应商 API；
- 需要维护幂等操作记录、计划明细活动标记、告警去重键以及计划明细到采购单明细的唯一关联；
- 需要额外维护下单尝试历史，区分明确失败、结果未知和查询确认不存在，换取可审计的安全重试；
- 把 `order_unknown` 计入采购承诺会暂时降低再次补货的数量，但这是避免重复采购的保守取舍，必须由后台恢复任务及时收敛；
- 同仓库/SKU 只允许一个活动建议、多供应商建单全有或全无，会牺牲部分并行处理能力，换取 V1 更清晰的防重边界；
- V1 假设基本单位为整数、单币种 CNY、预留量不超过现存量，且不支持已下达订单取消；不满足这些假设的数据必须阻断，不能把它们包装成已支持能力；
- 28 日基线、固定 `alpha=0.3` 和安全库存降级是可复现的 V1 基线，不代表已证明适合真实业务；
- 前端身份切换不等于真实认证；
- RAG 只做基础混合检索，重排和文档运营留到 V1.1；
- 合成数据不能证明真实库存成本或经营收益；
- Langfuse 云端观测依赖外部凭证；无凭证时保持本地结构化记录，云端 trace 未验证；
- CPU-only PyTorch 用于本仓库 CPU Embedding 场景，不提供 GPU 加速能力。

## 不采用的方案

| 方案 | 不采用原因 |
|---|---|
| LLM 直接生成补货数量 | 不可复现，数值与审计风险高 |
| 伪多 Agent 协作 | 增加复杂度但没有当前业务收益 |
| 文档数字运行时转参数 | 来源、版本和冲突不可验证 |
| LLM 直接审批/下单 | 副作用不可控，违反人工门槛 |
| 爬取数据替代供应商模型 | 脏数据、合规和业务关系缺失 |
| SQLite + 临时向量库 | 难以同时演示事务、向量和并发锁 |
| V1 部署 Milvus/独立搜索引擎 | 运维复杂度超过首版价值 |
| 安装 CUDA 版 PyTorch | 本仓库仅 CPU Embedding，CUDA 运行时徒增镜像体积（8.81GB → 2.21GB） |

## 版本边界

未明确指定版本时，实施和验收默认以 V1 为边界。V1.1/V2 可以作为路线规划被维护，但必须明确标注为未实现。

### V1

完整本机闭环：双触发、单 Agent、多轮澄清、混合检索、结构化规则、预测/补货/供应商工具、逐条审批、按供应商拆单、模拟供应商故障、未知状态恢复、分批收货、页面告警、审计、Langfuse（可选）、自动化测试、Docker Compose、发布基线（CPU-only 镜像、隔离全新部署验证、CI 无密钥可运行）。

### V1.1

公网部署、JWT/OAuth、规则文档上传/编辑/启停/回滚、Cross-Encoder、邮件/企业微信告警、灰度与容量演练。

### V2

动态安全库存、金额阈值审批、预算/供应商优化、多模型路由和更完整的线上评测。

## 修订记录

- 1.3 功能优化（2026-09-01）：发布收口第一轮用户功能优化——依赖可复现升级为双锁文件（`requirements.lock` base+dev、`requirements-rag.lock` base+rag，均以官方 CPU 源解析；`requirements-rag.lock` 内含 `torch==2.6.0+cpu` 且零 nvidia/triton/cuda-* 依赖，避免基于 PyPI CUDA torch 解析导致安装冲突或镜像膨胀）；前端错误可见性/恢复体验增强（阻断码→下一步映射、PLAN_STALE 变化字段、order_unknown 只查询、幂等键区分）；未改变任何核心架构决策。
- 1.3 审计收口（2026-09-01）：V1 发布候选审计与 CI 收口——pgvector 扩展改由 Alembic 迁移内 `CREATE EXTENSION IF NOT EXISTS vector` 确保（Compose/CI/裸机三场景可靠，CI 的 PostgreSQL service 不挂载 db-init 目录）；compose `env_file` 改 `required: false`（干净 CI 无 `.env` 可 config/build）；新增 `requirements.lock` 依赖锁文件（pip-tools，Dockerfile/CI 以 `--constraint` 应用，torch 仍由官方 CPU 源固定）；CI 密钥扫描只报文件名不泄露匹配内容、步骤顺序与 `POSTGRES_DSN` 一致性；评测 OFFLINE 模式强制禁用 LLM。CI 云端成功运行需 push 后由 GitHub Actions 执行（未提交仓库无云端记录）；未改变任何核心架构决策。
- 1.3 发布基线（2026-09-01）：V1 完整验收版发布基线收口——镜像交付采用官方 CPU-only PyTorch（2.6.0+cpu，仅 CPU Embedding，不装 CUDA 运行时，镜像 8.81GB → 2.21GB）；Langfuse 观测采用 2.x 低层 API 封装为可选组件（无凭证安全 no-op、脱敏、关联 request/trace/thread/plan id；本地 mock 单测通过，云端未验证不改变业务）；黄金集评测升级为 OFFLINE 与真实 LLM 双模式分表；隔离全新部署验证（独立 project/卷/端口）与 CI 无密钥可运行纳入 V1 验收；未改变任何核心架构决策。
- 1.2 收口（2026-09-01）：V1 本机收口验收——Docker Compose 容器化启动已实测（7 服务 Up、容器内迁移/种子幂等可重复、api 重启可重复启动）；Embedding 向量路径容器内实测通过；黄金集评测入口建立；修复 Agent `node_classify` 会话连接泄漏、pgvector 检索 ORM 绑定、seed 后 `alembic stamp head`、db 初始化 pgvector 扩展与 Dockerfile 构建优化；未改变任何架构决策。
- 1.2（2026-08-31）：V1 实现完成——记录两条实现性澄清：①定时扫描为每个有效仓库各生成一张待审批计划（计划按仓库维度，与活动建议部分唯一索引粒度一致）；②LangGraph checkpoint 在生产/Compose 使用 PostgreSQL，开发/测试使用内存后端（`CHECKPOINTER_BACKEND=memory`），业务状态始终以业务数据库为准。
- 1.1（2026-08-31）：确定计算顺序与数值规范、数据库审批权威、持久化恢复请求、决策新鲜度、活动建议与采购承诺双层防重、采购下单尝试/恢复、统一幂等/锁和定时逐 SKU 隔离。
- 1.0（2026-08-31）：接受核心架构决策。
