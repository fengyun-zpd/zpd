# StockMind - 可审计的智能仓储补货 Agent

> **文档版本**：V4.3
> **文档日期**：2026-09-01
> **当前状态**：V1 完整验收版（发布基线）——Docker Compose 容器化启动、CPU-only PyTorch 镜像、Langfuse 可选观测、黄金集评测（OFFLINE + 真实 LLM 双模式）与隔离全新部署验证齐备
> **目标岗位**：Agent 应用开发

StockMind 是一个面向中小制造与批发仓库的补货决策项目。系统允许操作员通过自然语言发起补货，也能由定时任务扫描低库存；它使用一个 LangGraph Agent 完成意图理解、多轮澄清、工具调用、规则检索和建议生成，再由人完成审批、创建采购单、模拟下单、分批到货和关闭。

本项目使用完全合成的数据和规则文档，不声称接入真实企业系统或产生真实经营收益。

## 1. 核心命题

补货决策同时包含非确定性问题与确定性问题，两者必须分开处理：

- Agent 负责理解用户、补齐上下文、选择和调用工具、检索证据、解释结果。
- RAG 负责找到可能适用的规则文档，但自然语言中的数字不能直接进入计算。
- 检索到的文档文本只作为不可信证据，文档中的提示、角色声明或工具指令不得改变 Agent 权限和工具白名单。
- 结构化规则服务负责校验规则的范围、版本、生效时间和冲突。
- 预测、补货计算和供应商选择由可测试的确定性工具完成。
- 审批、创建采购单、下单、未知状态恢复、收货、关闭/取消和管理配置只能由对应角色或内部任务明确操作，LLM 无权自主执行。

一句话概括：**Agent 负责理解和编排，领域服务负责计算与状态，人工负责高风险副作用。**

## 2. V1 可演示闭环

### 2.1 手动触发

```text
操作员自然语言发起
  -> Agent 提取仓库、SKU、规划窗口
  -> 缺少必要参数时多轮追问
  -> 查询库存、在途量和历史需求
  -> RAG 检索规则证据并解析到已校验的结构化规则
  -> 回测选择预测基线
  -> 确定性选择供应商
  -> 使用该供应商的交期、最小量和整箱倍数计算补货量
  -> 受控工具生成多 SKU 草稿并由领域服务提交待审批
  -> LangGraph checkpoint + interrupt 暂停会话
  -> 审批人通过页面/REST 逐条批准、排除或整单驳回
  -> resume 读取数据库中已校验的审批结果并恢复会话
  -> 采购员按供应商拆分采购单
  -> 采购员向模拟供应商 API 下达
  -> 操作员一次或多次登记到货
  -> 采购员确认关闭
```

### 2.2 定时触发

```text
Celery Beat 定时扫描
  -> 对每个有效仓库/SKU 执行同一套规则、预测和补货计算
  -> 自动生成待审批草稿
  -> 数据、规则或供应关系问题标记 blocked，运行异常标记 failed，并分别告警
  -> 其他 SKU 继续处理
  -> 后续审批与采购流程和手动触发一致
```

定时路径不进入 LangGraph 对话，也不使用 `interrupt/resume`；它直接复用领域查询、规则校验和确定性计算服务。定时与手动触发都生成执行记录，包括开始/结束时间、扫描量、草稿数、失败数和错误摘要。

**实现说明**：定时扫描为每个有效仓库各生成一张待审批计划（计划按仓库维度，与 `active_for_dedupe` 部分唯一索引的 (warehouse_id, product_id) 粒度一致）；一次扫描可能产生多张计划，执行记录与逐 SKU 执行明细保持完整血缘。

同一仓库/SKU 同时只允许一个尚未转成采购单的有效建议，由领域服务维护 `active_for_dedupe`。有效明细从草稿创建起参与防重，直到被排除、驳回、被修订替代，或在创建采购单的同一事务中关联到采购单明细。阻断明细不占用该唯一约束；同一阻断原因通过活动告警业务键去重，条件恢复后关闭告警。

每个计划明细保存决策输入哈希。审批时核对拟批准明细，创建采购单时核对全部已批准明细的库存、采购承诺、需求截止、规则、供应商关系、算法版本和仓库业务日；有变化就返回 `PLAN_STALE`，不部分执行、不悄悄改数量。审批人可以排除变化明细，需重算时由操作员生成整单修订版并把旧计划标记为 `superseded`。

若一次扫描没有任何有效明细，系统不创建空的待审批计划，只保存执行记录、阻断明细和告警。同一计划时间的重复调度、立即执行和手动重跑均通过执行幂等键防重。

## 3. 为什么它是 Agent 应用，而不是规则系统套聊天壳

V1 只使用一个“补货助手 Agent”，不包装伪多 Agent。Agent 实际承担以下非确定性工作：

- 识别补货、查询、解释等意图；
- 从自然语言中提取仓库、SKU 和 `7/14/30` 天规划窗口；
- 对缺失的必要参数主动发起多轮澄清；
- 根据当前会话选择只读工具并组织调用顺序；
- 检索适用规则的来源证据；
- 将确定性计算结果解释为业务人员可理解的建议；
- 使用 LangGraph checkpoint 保存会话状态，在审批处通过 `interrupt` 暂停，并在授权角色完成 REST 审批后通过 `resume` 读取已落库结果；checkpoint 不承担审批授权，也不是业务状态的事实源。

用户可以关闭非必要建议，但仓库、SKU、规划窗口等必要参数仍必须补齐；系统不能用猜测代替关键输入。

## 4. 确定性业务规则

### 4.1 预测

- 基线一：最近 28 个完整日的加权移动平均，权重从最早日的 1 递增到最近日的 28；
- 基线二：在同一 28 日窗口上使用固定 `alpha=0.3` 的简单指数平滑，以窗口最早日需求作为初值；
- 最近 28 个完整日做滚动单步回测；至少需要此前 28 日作为首个训练窗口，因此少于 56 个连续完整日时不做模型比较；
- 优先选择 WAPE 较低者，WAPE 相同则比较 MAE，仍相同则按固定算法 id 选择加权移动平均；验证期实际需求总和为 0 时只比较 MAE；
- 数据不足时不伪造销量预测，令预测覆盖需求为 0，按固定安全库存规则计算降级建议；
- 不使用 LLM 预测销量。

“完整日”要求数据源覆盖状态明确；确认无出库的日期补 0，来源缺失的日期不能当作 0，必须标记数据不足或阻断。所有阈值和参数随算法版本保存。

### 4.2 供应商选择

系统先排除不可供货的关系，再按以下稳定顺序选择：

```text
业务优先级 -> 交期 -> 采购价 -> supplier_id（稳定兜底）
```

数值越小优先，`supplier_id` 按字典序升序。供应商必须在补货量计算前选定，因为交期、最小采购量和整箱倍数来自该 SKU-供应商关系；无可用供应商时明细直接 `blocked`。V1 种子采购价统一使用 CNY，不做跨币种比较。Agent 可以解释选择原因，但不能改变确定性排序结果。审批通过后，有效计划明细按供应商聚合为多张采购单。

### 4.3 补货量

```text
可用库存 = 现存量 - 预留量
预测窗口 = max(用户规划窗口, 供应商交期 + 复查周期)
预测覆盖需求 = 日均预测 * 预测窗口
目标库存 = 预测覆盖需求 + 固定安全库存
有效在途量 = po_created、ordering、ordered、order_unknown 和 partially_received 采购单的剩余未收量
净需求 = max(0, 目标库存 - 可用库存 - 有效在途量)
订货量 = 净需求为 0 时取 0；否则 ceil(max(净需求, 最小采购量) / 整箱倍数) * 整箱倍数
```

V1 的安全库存来自已校验的结构化规则；按波动和服务水平动态计算安全库存放到 V2。

`order_unknown` 按保守原则计入有效在途量，直到查询供应商状态确认未创建并回到 `po_created`；此后采购员才可以重新下达或取消。这样可以避免在外部结果未知时重复采购。

`on_hand`、`reserved`、交期、复查周期、安全库存、最小采购量和整箱倍数必须为合法非负值，并满足 `reserved <= on_hand`、整箱倍数大于 0；不满足时阻断，不能静默截断或修正。

## 5. V1 技术方案

| 领域 | 选型 | 用途 |
|---|---|---|
| Web | React + TypeScript | 对话、审批、采购、任务与运行记录 |
| API | FastAPI + Pydantic v2 | REST/SSE、强类型契约、领域服务 |
| Agent | LangGraph | 会话状态、工具编排、interrupt/resume |
| 后台任务 | Celery Worker + Beat | 定时扫描、状态核对与告警 |
| 业务数据 | PostgreSQL | 事务、状态机、审计、幂等 |
| 向量检索 | pgvector | 本地中文 Embedding 的向量召回 |
| 关键词检索 | jieba + PostgreSQL 全文检索 | 中文词法召回 |
| 融合 | RRF | 向量与关键词结果融合 |
| 缓存/任务协调 | Redis | Celery broker、短期缓存、任务锁 |
| LLM | OpenAI 兼容接口，V1 验证 DeepSeek | 意图、工具编排和解释 |
| 观测 | Langfuse Cloud（可选，默认 no-op） | Trace、模型调用、Token、成本与耗时 |
| 部署 | Docker Compose | 本机一键启动完整依赖 |

V1 不部署独立搜索引擎，不做 Cross-Encoder 重排，不做多模型网关。

## 6. Web 工作区

V1 计划提供以下页面：

1. 补货助手：对话、澄清、参数确认、生成草稿；
2. 补货工作台：查看手动和定时生成的计划；
3. 审批箱：逐条批准或排除，并查看公式、数据快照和引用；
4. 采购单：创建、下达、未知状态恢复、分批到货、关闭，以及取消尚未下达的采购单；
5. 定时任务：配置扫描周期、立即执行和手动重跑；
6. 执行记录：查看处理量、错误、告警和关联 trace；
7. 规则知识库：只读查看文档、版本、范围、规则和引用；
8. 操作人切换器：选择演示用户，后端根据 `actor_id` 查询角色。

V1 的操作人切换器不是真实认证。JWT/OAuth 登录放到 V1.1。

## 7. 故障与安全演示

模拟供应商 API 支持切换以下故障：

- 正常成功；
- 明确失败；
- 超时但供应商实际已创建订单；
- 相同幂等键重复请求；
- 查询外部订单状态并恢复。

每次下达都保存不可变的下单尝试记录。供应商明确失败时采购单回到 `po_created`；超时或结果歧义时进入 `order_unknown`，后台只能先查询供应商状态。查询确认已创建后进入 `ordered`，确认不存在后回到 `po_created`，不能盲目重试。只有未开始外部下单的 `po_created` 可以取消；V1 不提供已下达订单的供应商取消。收货使用唯一 `receipt_event_id` 防止重复入库，且累计收货量不能超过采购单剩余量。

下单先在数据库中保存尝试并提交 `ordering`，再调用供应商。若进程在外呼前后崩溃或 `ordering` 超时，后台统一转入 `order_unknown` 并查询供应商，不能假定失败后直接重发。

`buyer` 可以手动触发未知订单查询，`system` 可以执行后台恢复任务；两条路径调用同一供应商适配器和状态迁移服务。`system` 是后台内部主体，不能由浏览器提交 `actor_id` 冒充；管理员触发任务时另行记录触发人。故障模式只允许 `admin` 通过页面或 REST 切换，Agent 无权查看为可调用工具或修改该模式。

所有状态变更命令均采用统一幂等契约：相同作用域、幂等键和请求载荷在完成后返回已保存结果；副作用发生前的瞬时失败可在同一操作上恢复；相同键但载荷不同返回稳定冲突错误；进行中的请求返回可查询的操作状态。审批、排除/驳回、建单、下单、未知状态恢复、收货、关闭/取消及管理配置都受事务、允许转换表和乐观版本校验保护。

## 8. 可验证证据

V1 使用固定随机种子生成虚构仓库、SKU、供应商、供货关系、规则和 90-180 天历史出库数据，并人工植入异常场景。评测不把所有问题压成一个“准确率”，而是分别报告：

- 意图与参数提取的字段级准确率；
- RAG 的 Recall@k、MRR 和引用正确率；
- 补货算法的示例、边界和性质测试；
- Agent 的任务完成率、工具调用正确率和必要澄清率；
- 预测的 MAE 和 WAPE；
- 越权操作成功数、重复采购数和重复入库数；
- 重复有效建议数、未知状态盲目重试数、非法状态迁移成功数和幂等键异载荷产生副作用数；
- P50/P95 延迟、Token 和单次运行成本。

黄金集评测（固定种子可复现）分 OFFLINE 与真实 LLM 双模式报告：确定性指标（参数/RAG/预测）两模式一致；对话指标分别按离线图与真实模型采样；P50/P95 与 Token 只在实际执行处采样；成本仅在有可配置单价时估算，否则只报 Token。越权操作、重复有效建议、重复采购、重复入库、未知状态盲目重试、非法状态迁移和幂等键异载荷副作用必须为 0。除此之外，文档不预填尚未实测的结果。

## 9. 版本边界

未明确指定版本时，实施和验收默认以 V1 为边界。V1.1/V2 可以作为规划被讨论和维护，但必须明确标注为未实现，不能作为 V1 已具备能力。

### V1：完整本机演示

- 双触发、单 Agent、多轮澄清；
- 混合检索和结构化规则解析；
- 预测、补货和供应商确定性工具；
- 细粒度审批、按供应商拆单；
- 模拟下单、未知状态恢复、分批到货和关闭；
- 页面内告警、审计、Langfuse trace（可选）；
- Docker Compose、自动化测试、核心评测和 GitHub Actions；
- 发布基线：CPU-only PyTorch 镜像、隔离全新部署验证、CI 无密钥可运行。

### V1.1：运营与交付增强

- 公网部署；
- JWT/OAuth；
- 规则文档上传、编辑、启停和回滚；
- Cross-Encoder 重排；
- 邮件或企业微信告警；
- 更完整的容量与故障演练。

### V2：信任与算法增强

- 基于需求波动与服务水平的动态安全库存；
- 经业务验证后的金额阈值自动审批；
- 供应商容量、预算和价格优化；
- 多模型路由与更复杂的线上评测。

## 10. 诚实边界

V1 完整验收版已实现：Docker Compose 环境、FastAPI 领域服务与 API、PostgreSQL 迁移/约束/种子、Redis+Celery、LangGraph 对话（interrupt/resume，真实 LLM 与离线双模式均验证）、RAG 与结构化规则、确定性计算、前端八页面、单元/性质/集成测试、HTTP 冒烟与故障注入验证、黄金集双模式评测、隔离全新部署验证均已落地。项目不声称：

- 使用真实企业数据；
- 已接入真实 WMS、ERP 或供应商；
- 具备生产级认证、多租户或高并发能力；
- 真实 LLM 评测结果非确定性，小样本延迟不构成容量结论；
- Langfuse 云端 trace 已验证（未配置凭证；代码已实现、本地 mock 已验证、云端未验证）；
- 已达到任何尚未实测的准确率、延迟或成本目标（未配置 LLM Key 时对话运行离线演示模式，不调用真实 LLM；配置 Key 时使用真实模型并已实测）；
- 是完整 WMS、采购平台或企业级 Agent 平台。

## 11. 文档索引

- [需求规格说明书 V4.3](StockMind需求规格说明书.md)
- [架构设计文档 V4.3](StockMind架构设计文档.md)
- [ADR 001 修订版 1.3：核心架构边界与基础设施](StockMind架构决策记录ADR001.md)
- [工作区 Agent 宪法 v1.3](AGENTS.md)

## 12. 修订记录

- V4.3 功能优化（2026-09-01）：发布收口第一轮用户功能优化——补货助手与审批箱错误可见性/恢复体验增强（缺参明确列出需补充字段、阻断原因+下一步动作、PLAN_STALE 变化字段+排除/重算入口、order_unknown 只查询恢复提示、幂等键区分原结果/异载荷冲突、对话模式与 RAG 状态区分展示）；新增 `backend/requirements-rag.lock` 完整锁定 base+rag（torch 固定官方 CPU 源 2.6.0+cpu，零 nvidia/triton/cuda-* 依赖），修复"requirements.lock 仅覆盖 base+dev"的发布基线风险；新增 6 项单元测试与 3 个 Playwright 场景；全套测试 122 项、7 项安全不变量全为 0。未改变任何领域公式、权限边界、状态机与数据库事实源。
- V4.3 审计收口（2026-09-01）：V1 发布候选审计与 CI 收口——修复 CI 真实阻塞（Alembic 迁移内 `CREATE EXTENSION IF NOT EXISTS vector` 使 Compose/CI/裸机三场景可靠；compose `env_file: required: false` 使干净 CI 无 `.env` 可 config/build；CI 密钥扫描只报文件名不泄露匹配内容；CI 步骤顺序与 `POSTGRES_DSN` 一致性）；新增 `backend/requirements.lock` 依赖锁文件（pip-tools，Dockerfile/CI 以 `--constraint` 应用，torch 仍由官方 CPU 源固定 2.6.0+cpu）；评测 OFFLINE 模式强制禁用 LLM（此前误用真实模型）；本地等价验证 CI backend 全流程与容器业务语义复验通过。CI 云端成功运行需 push 后由 GitHub Actions 执行（仓库未提交，无云端运行记录，如实标注）。
- V4.3 发布基线（2026-09-01）：V1 完整验收版发布基线收口——后端镜像瘦身（CPU-only PyTorch 2.6.0+cpu，8.81GB → 2.21GB，零 CUDA/nvidia 包，重建后容器内验证 embedding/检索/对话/e2e 正常）；Langfuse 可选观测实现（无凭证安全 no-op、脱敏、关联 request/trace/thread/plan id，本地 mock 单测通过，云端未验证——唯一外部阻塞）；黄金集评测升级为 OFFLINE 与真实 LLM 双模式分表报告（真实 LLM 对话 3 例：任务完成率 0.667、工具调用正确率 0.929、P50 1.68s、Token 总量 859，单价未配置只报 Token）；隔离 Compose 全新部署验证通过（独立 project/卷/端口：迁移/种子幂等、pgvector 扩展、Redis/PG/Worker/Beat 连通、OFFLINE 对话、Embedding 16 条向量、HTTP 冒烟闭环、容器 nginx 前端 Playwright E2E、停止重启恢复）；CI 增加容器构建检查、密钥扫描、格式检查与迁移/种子幂等步骤；全套后端测试 116 项通过、7 项安全不变量全为 0。
- V4.2 收口（2026-09-01）：本机收口验收——Docker Compose 容器化启动已实测（7 服务 Up、容器内迁移/种子幂等可重复）；Embedding 向量路径容器内实测通过；黄金集评测入口建立；真实 LLM 验证通过；安全不变量 7 项全为 0；修复 Agent 会话连接泄漏、pgvector 检索绑定、seed 后 alembic stamp、db 初始化扩展与 Dockerfile 构建优化。
- V4.2（2026-08-31）：V1 本机闭环实现完成——补充实现说明（定时扫描按仓库建计划、种子场景调整）、修正"代码尚未实现"状态描述。
- V4.1（2026-08-31）：收敛版本边界、预测/供应商/补货确定性算法、决策新鲜度、活动建议防重、人工审批恢复、采购状态机、外部下单恢复、收货事务、统一幂等与定时故障隔离。
- V4.0（2026-08-31）：确认 V1 完整本机闭环设计。

License：MIT，见 [LICENSE](LICENSE)。
