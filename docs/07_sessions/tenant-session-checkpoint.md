# 租户、会话与线程管理

`thread_id` 必须由后端生成不透明值；客户端只能引用自己租户内的线程。`approval_id`、`operation_id` 与 `thread_id` 独立，避免会话重试导致业务重复。

checkpoint 保存对话恢复状态，不是业务事实源。恢复前重新读取数据库状态、版本和权限；并发 resume 使用版本条件更新。线程超时、取消、人工接管和恢复失败都写审计。
