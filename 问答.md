# StockMind 项目熟悉计划与面试问答

> 适用项目：StockMind 可审计的智能仓储补货 Agent，V1 冻结基线。
> 学习目标：能够从业务价值、架构边界、数据一致性、安全、评测和运维六个角度解释项目，并能现场演示“缺参澄清 -> 草稿 -> 人工审批 -> PLAN_STALE -> 建单 -> 下单未知状态恢复 -> 收货 -> 审计回放”。

## 先记住三条事实

1. 这是仓储补货系统，不是医疗系统；数据是固定随机种子生成的合成数据，没有接入真实 WMS、ERP 或供应商。
2. Agent 只负责理解、澄清、检索和编排；预测、供应商选择、补货量和状态迁移由确定性领域服务完成；审批、下单、收货等副作用由授权人员或内部任务触发。
3. V1 是本机 Docker Compose 演示和验收基线，不应在面试中声称具备生产级认证、多租户、高并发或真实经营收益。

## 学习方法

每个阶段先读文档，再沿代码入口走一条真实请求，最后用问题复述设计取舍。建议每阶段半天到一天；十个阶段完成后，应能不看稿完成五分钟演示，并能解释一个失败场景的恢复路径。

---

## 阶段一：业务定位与闭环

### 学习目标

能说清楚系统解决什么问题、谁使用、哪些动作必须人工确认，以及为什么它不是“规则系统套一个聊天界面”。

### 重点阅读与代码

- `StockMind项目说明.md` 第 1、2、3、9、10 节。
- `StockMind需求规格说明书.md` 第 0-3、6、11 节。
- `docs/12_interview/demo-runbook.md`。
- `backend/app/api/conversations.py`、`backend/app/agent/graph.py`、`backend/app/services/plan_service.py`。

### 核心概念

补货建议、计划与采购单分离、人工审批、确定性服务、Agent 编排、合成数据、V1/V1.1/V2 边界、可审计性。

### 面试问题与高质量解答

**1. 这个项目解决什么业务问题？价值是什么？**

- 结论：它把库存、需求、在途、供应商约束和规则证据组合成可解释的补货建议，并把建议推进到审批、采购、下单、收货和审计闭环。
- 为什么这样做：补货不是只预测销量，还要处理最小采购量、整箱倍数、交期、在途承诺和规则冲突。只给一个模型数字无法支撑采购责任。
- 项目实现：用户可以自然语言发起补货，也可以由 Celery 定时扫描低库存；计划明细保存中间量、规则引用、输入快照和 `decision_input_hash`，采购和收货状态写入 PostgreSQL。
- 不这样做的后果：建议无法解释，库存变化后仍可能按旧数量下单，重复采购或漏采无法追责。
- 改进方向：真实接入 WMS/ERP 前，应先定义数据新鲜度、对账、租户隔离和回滚演练，不能直接把合成数据方案当生产系统。

**2. 为什么说它是 Agent 应用，而不是规则系统加聊天壳？**

- Agent 承担非确定性工作：识别意图、提取仓库/SKU/规划窗口、缺参澄清、选择只读工具、检索证据和解释确定性结果。
- 计算顺序和数值结果不交给模型；`plan_service.py` 调规则、预测、供应商和补货服务，模型不能改写数量或供应商。
- 用户体验因此更自然，但结果仍能回放。若把所有内容塞进 Prompt，模型可能编造数字、绕过权限或在输入变化后静默重算。

**3. 手动触发和定时触发有什么区别？**

- 手动路径进入 LangGraph，缺参时多轮澄清，生成草稿后以 checkpoint + `interrupt` 暂停，审批后由 `resume` 读取数据库决定。
- 定时路径由 Celery Beat/Worker 直接调用领域服务，不进入对话 Agent，也不使用 checkpoint；每个 SKU 独立处理，问题标记 `blocked` 或 `failed`，其他 SKU 继续。
- 这样分离是为了让批处理可预测、可重试，避免后台任务依赖会话状态。统一之处是两条路径复用同一套规则、预测、供应商、补货和状态服务。

---

## 阶段二：总体架构与模块边界

### 学习目标

能画出请求从前端到 API、Agent、RAG、领域服务、数据库和后台任务的路径，并说明每个模块拥有的责任。

### 重点阅读与代码

- `StockMind架构设计文档.md` 第 1-4、10-12 节。
- `StockMind架构决策记录ADR001.md` 决策 1-8。
- `backend/app/main.py`、`backend/app/api/`、`backend/app/agent/`、`backend/app/services/`、`backend/app/tasks/`。

### 核心概念

分层架构、API 与领域服务、单 Agent、工具白名单、数据库事实源、checkpoint、Redis broker、PostgreSQL 事务、反腐层。

### 面试问题与高质量解答

**1. 请描述一次手动补货请求的调用链。**

- React 页面通过 SSE/REST 将消息交给 FastAPI；API 根据 `X-Actor-Id` 建立请求上下文并校验会话归属。
- LangGraph 的 `classify` 判断意图和参数，不足则 `clarify`；参数完整后调用只读工具检索库存、需求、供应商和规则证据，再调用受控的生成草稿工具。
- 草稿工具进入 `plan_service.py`，执行规则校验、预测、供应商选择、补货量计算、幂等和活动建议防重，并提交 `pending_approval`。
- Agent 只在领域服务成功落库后 `interrupt`；审批由 REST 领域接口写库，Celery 触发恢复，`resume` 只读决定。

**2. 为什么 V1 只用一个 Agent？**

- ADR 001 选择单 Agent，因为当前任务的工具边界和指标可以在一个图内验证；拆多 Agent 会增加共享状态、权限、超时、重试和轨迹回放复杂度。
- 单 Agent 不等于把写权限交给模型，工具仍按只读/受控草稿白名单限制。
- 未来只有当职责、Schema、失败回退和指标收益都可验证时才拆分；CrewAI 等规划能力不能直接获得数据库写权限。

**3. 为什么业务数据库是事实源，LangGraph checkpoint 不是？**

- checkpoint 保存会话上下文和图恢复位置，可能丢失、重复或与业务事务不同步；业务状态必须由 PostgreSQL 的事务、约束和版本号决定。
- 审批先写 `plan_line`/审批结果和 `workflow_resume`，再异步恢复；恢复失败只告警，不回滚已经提交的业务决定。
- 如果反过来以 checkpoint 作为审批凭证，重复 resume、人工 REST 审批和数据库状态会产生分叉，无法审计。

---

## 阶段三：数据模型与数据库设计

### 学习目标

能解释领域实体、主外键、约束、索引、版本字段和为什么计划与采购单必须分开。

### 重点阅读与代码

- `StockMind架构设计文档.md` 第 4、7 节。
- `backend/app/models/inventory.py`、`replenishment.py`、`purchasing.py`、`governance.py`、`knowledge.py`、`agent.py`。
- `backend/alembic/versions/` 与 `backend/app/seed/seed.py`。

### 核心概念

规范化、领域边界、部分唯一索引、CHECK 约束、乐观版本、不可变尝试记录、审计、幂等操作、快照与血缘。

### 面试问题与高质量解答

**1. 为什么 `replenishment_plan` 和 `purchase_order` 分开？**

- 计划是“建议与审批事实”，采购单是“采购承诺与外部副作用”。计划可以被排除、驳回或修订，批准也不等于已经建单。
- 一个计划可能按供应商拆为多张采购单；`purchase_order_line.plan_line_id` 唯一约束保证一个有效计划明细最多落实一次。
- 混成一张表会把审批状态和外部订单状态耦合，无法表达多供应商拆单、PLAN_STALE 和下单未知状态。

**2. `decision_input_hash` 解决什么问题？**

- 它对业务日、库存/预留版本、需求截止日、规则、供应商关系、算法版本等规范化 JSON 做 SHA-256，代表本次建议依赖的输入快照。
- 审批和建单前重新计算；任何输入变化返回 `PLAN_STALE`，不部分执行、不静默重算。
- canonical JSON 规定键排序、集合稳定排序、Decimal 无指数字符串、时区 ISO 时间和显式 `null`，否则同一业务输入可能因序列化差异产生不同哈希。

**3. 数据库如何防重复和并发冲突？**

- 活动建议使用 `(warehouse_id, product_id) WHERE active_for_dedupe` 部分唯一索引；采购明细对 `plan_line_id` 唯一；收货使用全局唯一 `receipt_event_id`。
- 更新命令带幂等键和乐观 `version`；同键同载荷返回原结果，同键异载荷返回 409，版本不一致返回 `VERSION_CONFLICT`。
- 影响同一仓库/SKU 的操作按稳定顺序取得 PostgreSQL advisory lock，锁内做版本和状态校验。Redis 锁只协调任务，不能作为业务正确性来源。

---

## 阶段四：规则引擎、确定性计算与供应商选择

### 学习目标

能从公式和调用顺序解释补货量，说明规则冲突、非法输入和 RAG 数字不能直接计算的原因。

### 重点阅读与代码

- `StockMind项目说明.md` 第 4 节。
- `backend/app/services/rules.py`、`forecast.py`、`supplier.py`、`replenishment.py`、`plan_service.py`。
- `backend/app/rag/rule_parser.py`、`backend/app/models/replenishment.py`。

### 核心概念

规则作用域、版本与生效期、同级冲突、WAPE/MAE、降级计算、确定性排序、Decimal、最小采购量、整箱倍数、在途抵扣。

### 面试问题与高质量解答

**1. 规则解析和计算为什么必须分开？**

- RAG 只找候选文本，`rule_parser.py` 只做受限数字抽取，`rules.py` 再校验类型、作用域、版本、生效期和冲突，只有校验后的结构化规则进入计算。
- 这样可以把自然语言中的提示注入、角色声明或伪造数字当作不可信证据。如果让 LLM 直接把“安全库存 100”写进计算，任何文档或用户输入都可能越权改变结果。
- 代价是需要维护结构化规则和文档血缘，但换来了可测试、可回放和可审计。

**2. 补货量公式是什么，为什么先选供应商？**

- `available = on_hand - reserved`；`window = max(requested_window, lead_days + review_period_days)`；`target = daily_forecast * window + fixed_safety_stock`；扣除有效在途得到 `net`；净需求大于 0 时按最小采购量和整箱倍数向上取整。
- 供应商必须先选，因为交期、最小采购量和整箱倍数来自 SKU-供应商关系。供应商按业务优先级、交期、价格、`supplier_id` 稳定排序。
- 若先算量再选供应商，换供应商可能改变覆盖窗口和取整，原数量就没有可解释性。

**3. 数据不足时为什么不预测，而是安全库存降级或阻断？**

- 预测要求至少 56 个连续完整日：28 日训练窗口加 28 日滚动验证；明确无出库可补 0，来源缺失不能伪造为 0。
- 数据不足时返回 `fixed_safety_stock_fallback`，预测和误差指标为 null，使用已校验的固定安全库存；固定安全库存也缺失则阻断。
- 这样牺牲部分自动化换取诚实性。如果把缺失数据当 0，可能产生过低订货量并掩盖数据源故障。

---

## 阶段五：RAG、检索评分与 ICD 边界题

### 学习目标

掌握中文混合检索、RRF 评分、引用正确率和提示注入防护；同时能正确回答项目没有 ICD 编码模块这一事实。

### 重点阅读与代码

- `StockMind架构设计文档.md` 第 5 节。
- `backend/app/rag/embedding.py`、`fts.py`、`fusion.py`、`retriever.py`、`rule_parser.py`。
- `backend/app/api/knowledge.py`、`backend/app/services/knowledge_import_service.py`。
- `docs/04_rag_memory/rag-memory.md`、`docs/08_evaluation/eval-and-replay.md`。

### 核心概念

Embedding、pgvector、PostgreSQL FTS、jieba、Top-K、RRF、Recall@K、MRR、引用正确率、groundedness、提示注入、文档血缘。

### 面试问题与高质量解答

**1. 为什么采用向量检索 + 关键词检索 + RRF？**

- 中文业务规则既有语义表达，也有 SKU、规则编号、日期和数值等精确词；单独向量检索可能漏掉编号，单独关键词检索又不擅长同义表达。
- 先分别取得向量和 FTS 排名，再用 Reciprocal Rank Fusion 融合，不要求两路分数同量纲；随后按启用、版本、作用域和生效期过滤。
- V1 选择 pgvector 是因为数据规模是合成演示，减少独立搜索集群运维。规模上升后可评估专用搜索、重排模型和离线索引构建。

**2. 如何评价检索质量？**

- Recall@K 衡量正确证据是否进入前 K；MRR 关注第一个正确结果的位置；引用正确率检查回答引用是否真实、启用且适用。
- 黄金集包含规则冲突、缺参、提示注入和伪造引用；评测分别报告 RAG 指标，不把检索质量和对话完成率压成一个准确率。
- 当前 V1 文档记录过 Recall@5=0.8、MRR=0.8、引用正确率=1.0，这些是固定黄金集结果，不能外推生产效果。

**3. 面试官问“ICD 编码检索和评分怎么做”，你如何回答？**

- 先澄清：StockMind 是仓储补货项目，当前代码、数据模型和验收范围没有 ICD-10 编码、诊断、医疗文本或医疗评分模块，不能声称已经实现。
- 若讨论可迁移设计，可采用“术语规范化 -> 候选编码召回 -> 版本/适用范围过滤 -> 精确匹配与语义相似度融合 -> 引用原始医学资料 -> 人工确认”的流程；评分要分召回率、Top-1/Top-K 准确率、拒识率和版本一致性，不能让 LLM 直接决定诊断编码。
- 医疗场景还要做最小权限、脱敏、访问审计、数据留存和人工复核。把这套能力接入本项目应进入新的医疗业务插件和 ADR，不能塞进 V1。

---

## 阶段六：Agent、LLM 边界与降级

### 学习目标

能解释 LangGraph 状态图、工具白名单、离线模式、真实模型模式和模型失败时的行为。

### 重点阅读与代码

- `backend/app/agent/graph.py`、`state.py`、`tools.py`、`llm.py`、`offline.py`、`blocked_hints.py`。
- `backend/tests/agent/` 与 `backend/tests/integration/test_conversation_ownership.py`。
- `StockMind需求规格说明书.md` 第 3 节、`ADR001` 决策 1 和 7。

### 核心概念

结构化状态、工具调用、interrupt/resume、离线确定性、模型路由、超时、拒答、越权防护、输入长度限制、脱敏。

### 面试问题与高质量解答

**1. LLM 可以调用哪些写工具？**

- 唯一允许的是生成补货草稿，且参数强类型，必须进入领域服务做权限、规则、数量、新鲜度、活动建议唯一性和幂等校验。
- 审批、建单、下单、未知状态查询、收货、关闭/取消、规则修改、任务配置和故障切换都不是 Agent 工具。
- 这样把自然语言的不确定性隔离在低风险阶段；如果允许模型直接下单，提示注入、重复调用或幻觉都会产生真实副作用。

**2. 真实 LLM 不可用时如何降级？**

- 未配置 LLM Key 时进入 OFFLINE 演示模式，使用确定性图和固定结果；页面标注 offline，不冒充真实模型。
- 规则、预测、补货和状态服务始终可用，模型只影响意图理解和解释。真实模型超时或结构化输出失败时应返回可见错误或人工路径，不能偷偷使用不一致的计算。
- OFFLINE 模式用于 CI 和确定性评测；真实 LLM 另行报告，因为采样、延迟和 Token 不可完全复现。

**3. PLAN_STALE 为什么必须阻断而不是自动重算？**

- 审批或建单前重新计算输入哈希，库存、在途、规则、供应商关系或算法版本变化就返回 409，并列出变化字段。
- 自动重算会让审批人批准的数量与实际执行数量不同；部分重算还会破坏多 SKU 计划的整体语义。
- 操作员必须明确排除变化明细，或通过受控草稿工具创建整单修订版；旧计划变为 `superseded`，新旧关系进入审计。

---

## 阶段七：采购状态机、Celery 与外部未知状态

### 学习目标

掌握状态迁移、异步任务、幂等键和跨系统副作用恢复，这是面试中最能体现后端工程能力的部分。

### 重点阅读与代码

- `backend/app/tasks/celery_app.py`、`scan.py`、`recovery.py`、`resume_trigger.py`。
- `backend/app/services/purchase_service.py`、`execution_service.py`、`locks.py`、`idempotency.py`。
- `backend/app/mock_supplier/app.py`、`backend/tests/integration/test_plan_flow.py`、`test_schedule_scan.py`。

### 核心概念

Celery Beat/Worker、任务幂等、任务锁、状态机、先持久化后外呼、order_unknown、对账、最终一致性、Outbox-like workflow_resume。

### 面试问题与高质量解答

**1. 下单超时为什么进入 `order_unknown`？**

- 外部请求可能已经创建订单，只是响应丢失；若当成失败换新幂等键重试，会造成重复采购。
- 系统先在数据库提交 `purchase_order_attempt` 和 `ordering`，外呼后按成功、明确失败或歧义回写。超时、断连和进程崩溃统一收敛为 `order_unknown`。
- 恢复只能使用原供应商幂等键查询；确认已创建进入 `ordered`，确认不存在回到 `po_created` 后才允许新尝试。这个协议用对账取代不存在的跨系统事务。

**2. Celery 在项目中负责什么，不负责什么？**

- Beat 根据带 IANA 时区的配置触发扫描，Worker 执行每个仓库/SKU 的领域服务；恢复任务处理陈旧 `ordering`、未知订单查询和会话 resume。
- Celery 是调度和异步执行层，Redis 是 broker/协调锁；它们不拥有业务状态，不替代 PostgreSQL 事务和唯一约束。
- 单 SKU 失败写 `execution_item` 为 `blocked` 或 `failed` 并告警，不能阻断整个扫描。任务可重跑但必须复用业务幂等键。

**3. 审批落库后为什么还要持久化 `workflow_resume`？**

- 如果先提交审批再直接发消息，进程可能在提交后、入队前崩溃，业务决定已存在但对话不恢复。
- 审批事务同时写 `workflow_resume`，提交后由 Celery 触发；恢复键是 `thread_id + plan_id + decision_version`，重复恢复返回已有结果，失败告警但不回滚审批。
- 这是业务事实和会话恢复解耦的做法，后续可升级为更完整的 Outbox/消息投递确认，但 V1 已覆盖演示闭环。

---

## 阶段八：安全、权限、审计与合规

### 学习目标

能说明演示身份边界、角色矩阵、审计字段、脱敏策略和医疗问题的正确边界答法。

### 重点阅读与代码

- `backend/app/services/access.py`、`audit.py`、`observability.py`、`api/deps.py`、`api/governance.py`。
- `StockMind需求规格说明书.md` 第 1、6、10、11 节。
- `docs/10_operations/production-baseline.md`、`docs/08_evaluation/eval-and-replay.md`。

### 核心概念

RBAC、主体身份、`X-Actor-Id`、system 内部主体、最小权限、审计不可抵赖、脱敏、提示注入、合规边界、医疗隐私。

### 面试问题与高质量解答

**1. V1 的认证和授权边界是什么？**

- V1 用 `X-Actor-Id` 查询种子用户角色，是演示身份切换，不是真实认证；JWT/OAuth 放在 V1.1。
- 请求体中的 `actor_id` 不能改变请求身份；`system` 只能由 Celery 内部使用，浏览器不能冒充。创建、读取和发送会话还要校验 `thread_id` 归属。
- 领域服务再次校验角色和状态，不能只相信前端按钮。生产改造需要真实认证、租户隔离、密钥管理和策略中心。

**2. 审计日志应该记录什么，如何避免泄露？**

- 记录 actor、命令、对象、幂等键、载荷哈希、状态前后值、输入哈希、算法/规则版本、trace/request/operation id 和错误码。
- 不记录密钥、完整 Prompt 和不必要的敏感原文；日志和 Langfuse 做脱敏。业务事务不能依赖观测系统，Langfuse 不可用时仍能完成交易。
- 只有自然语言解释没有执行记录是不够的，出了重复采购或非法迁移无法重放和追责。

**3. 如果面试官要求医疗隐私和 ICD 合规，如何回答？**

- 明确当前项目没有医疗数据、ICD 编码、诊疗建议或 HIPAA/等保合规声明，不能把仓储项目包装成医疗系统。
- 跨领域设计应增加数据分类、字段级脱敏/加密、最小权限、访问审计、保留删除策略、密钥轮换、人工复核和拒识机制；ICD 编码还要锁定版本和适用国家/地区。
- 这些是未来医疗插件的需求，不属于 V1 冻结边界；贸然加入会破坏事实源和 ADR 约束。

---

## 阶段九：测试、评估与可观测性

### 学习目标

能区分单元、性质、集成、E2E、黄金集和安全不变量，并能解释指标为什么分层报告。

### 重点阅读与代码

- `backend/tests/unit/`、`integration/`、`agent/`、`e2e/`。
- `backend/evaluation/`、`docs/08_evaluation/eval-and-replay.md`、`.github/workflows/`。
- `backend/app/observability.py`、`services/audit.py`、`services/execution_service.py`。

### 核心概念

测试金字塔、Hypothesis、Playwright、黄金集、字段准确率、Recall/MRR、MAE/WAPE、P50/P95、Token、成本、安全不变量、trace。

### 面试问题与高质量解答

**1. 为什么不只报告一个“准确率”？**

- 参数提取、RAG、预测、工具调用、任务完成和安全正确性是不同故障面；一个总分会掩盖“引用对但数量错”或“功能对但越权”的问题。
- 项目分别报告参数字段准确率、RAG Recall/MRR/引用正确率、MAE/WAPE、任务完成率、工具调用正确率、必要澄清率、P50/P95、Token/成本和七项安全不变量。
- OFFLINE 和真实 LLM 分表；确定性指标应一致，对话指标受采样影响，不能把三例真实模型结果当容量结论。

**2. 如何验证关键安全不变量？**

- 用集成测试覆盖越权、重复有效建议、重复采购、重复收货、未知状态盲目重试、非法状态迁移和幂等键异载荷副作用，预期均为 0。
- 数据库唯一约束和事务保证最终正确性，测试检查 API 状态码、数据库行数、审计和外部模拟供应商调用次数。
- 只测 HTTP 200 不够，因为副作用可能已经发生；必须检查“没有创建任何采购单/没有第二次外呼”等负向结果。

**3. 如何定位线上一次慢请求？**

- 用 `request_id/trace_id/thread_id/operation_id` 串起 API、Agent、RAG、领域服务、Celery 和供应商调用；记录脱敏日志、阶段耗时、Token、错误码和数据库查询信息。
- 先区分 LLM 延迟、Embedding/FTS、锁等待、数据库慢查询、Redis/Celery 排队还是供应商超时，再针对性优化。
- Langfuse 是可选观测，云端未验证；没有凭证时安全 no-op，不能让观测故障阻断业务。

---

## 阶段十：部署、运维、性能与失败案例

### 学习目标

能说明 Docker/CI/迁移回滚路径，提出有证据的性能优化，并准备一到两个诚实的失败案例。

### 重点阅读与代码

- `docker-compose.yml`、`docker-compose.iso.yml`、`backend/Dockerfile`、`frontend/`。
- `backend/alembic/`、`backend/requirements.lock`、`backend/requirements-rag.lock`、`.github/workflows/`。
- `docs/10_operations/production-baseline.md`、`scripts/verify_interview.ps1`。

### 核心概念

Compose、CPU-only 镜像、可复现依赖、Alembic、迁移幂等、回滚、连接池、索引、缓存、容量压测、备份恢复、故障注入。

### 面试问题与高质量解答

**1. Docker 和 CI 如何保证环境可复现？**

- Compose 编排 API、Worker、Beat、PostgreSQL/pgvector、Redis、前端和模拟供应商；依赖使用锁文件，Embedding 使用 CPU-only PyTorch，避免 CUDA 镜像膨胀。
- pgvector 扩展由 Alembic 迁移 `CREATE EXTENSION IF NOT EXISTS vector` 管理，迁移和种子可重复；CI 在无真实密钥时强制 OFFLINE。
- 发布前运行隔离全新部署、健康/就绪检查、HTTP/Playwright 冒烟和关键测试；生产还需要备份恢复、迁移回滚、容量压测和模型回滚证据，当前未声称具备。

**2. 你会如何优化性能？**

- 数据库：为仓库/SKU、计划、采购单状态、attempt 状态和 FTS/向量检索建立匹配索引；避免 N+1，使用批量查询和合理连接池。
- 检索：缓存稳定的文档 Embedding 和短期规则候选，但缓存键必须包含文档版本、作用域和租户；计算结果不能用无版本缓存替代事实源。
- 异步：把扫描、Embedding 和未知状态恢复放 Worker；控制并发、锁等待和外部供应商超时。任何优化都要重新验证幂等和 `PLAN_STALE`。

**3. 讲一个失败案例和改进。**

- 典型案例是下单超时：第一版如果把超时当失败并换幂等键重试，供应商可能已经建单，造成重复采购。
- 改进为先落库 attempt，再外呼；超时进入 `order_unknown`，只用原供应商幂等键查询，确认不存在后才允许新尝试。测试覆盖超时已创建、超时未创建和重复查询。
- 另一个案例是严格验收入口曾因 PowerShell 参数传递不可靠而误检查主端口；修正后改用明确的命名开关，并让关键 pytest/E2E 出现任何 skip 都失败。这说明验收脚本本身也需要验证。

---

## 五分钟面试演示顺序

1. 打开 `http://127.0.0.1:13000`，先输入“帮我补货”，展示缺少仓库、SKU 和规划窗口时的业务语言澄清。
2. 提供仓库、SKU 范围和 7/14/30 天窗口，展示规则引用、预测/供应商/数量中间量和草稿审计。
3. 在审批箱逐条批准或排除，强调 Agent 没有审批和建单权限。
4. 改变库存或规则后尝试建单，展示 409 `PLAN_STALE`、变化字段和无部分副作用。
5. 按供应商建单，切换模拟供应商超时，展示 `order_unknown`；使用原幂等键查询恢复，不能盲目重下。
6. 分两次收货，展示 `partially_received -> received -> closed`、重复 `receipt_event_id` 被拦截，以及审计/执行记录回放。

验收入口：`powershell -ExecutionPolicy Bypass -File .\scripts\verify_interview.ps1 -StopAfter`。它会重置隔离 Compose 环境，运行关键后端测试和完整 E2E；关键测试有任何跳过都会失败。

---

## 面试前自检清单

- [ ] 能用 30 秒说清业务价值、用户角色、V1 边界和合成数据事实。
- [ ] 能画出 UI -> FastAPI -> LangGraph/RAG -> 领域服务 -> PostgreSQL 的主链路。
- [ ] 能说出 Agent 允许的唯一写工具，以及为什么不允许审批、下单和收货。
- [ ] 能写出补货量公式，并解释供应商必须先于数量计算选择。
- [ ] 能解释规则优先级 `SKU > 分类 > 仓库 > 全局`、同级冲突和数据不足降级。
- [ ] 能解释计划、计划明细、采购单、下单尝试和收货事件的关系。
- [ ] 能解释 `decision_input_hash`、部分唯一索引、乐观版本和同键异载荷冲突。
- [ ] 能完整讲完 `order_unknown` 的查询恢复协议，并说明为什么不能换键重下。
- [ ] 能说清 Celery Beat/Worker 与 Redis 的职责，以及定时路径不进入 LangGraph。
- [ ] 能区分 OFFLINE/真实 LLM 评测，解释 Recall、MRR、MAE、WAPE、P50/P95 和七项安全不变量。
- [ ] 能说明 V1 的 `X-Actor-Id` 只是演示身份，不是真实认证；知道 JWT/OAuth 属于 V1.1。
- [ ] 能回答 ICD/医疗隐私题时明确“当前项目没有该模块”，并给出未来插件的安全设计。
- [ ] 能完成五分钟演示，并在每一步指出数据库事实源、人工动作和审计证据。
- [ ] 能说出至少一个真实边界：Langfuse 云端未验证、无真实 WMS/ERP、无生产高并发结论。

## 事实边界速查

| 可以明确说 | 必须谨慎说 |
|---|---|
| V1 本机 Docker Compose 闭环已验证 | 生产级认证、多租户、高并发：未实现/未验证 |
| 规则、预测、供应商和补货由确定性服务完成 | 真实企业节省成本或经营收益：不能声称 |
| 7 项安全不变量专项检查为 0 | Langfuse 云端 trace：代码和本地 mock 已验证，云端未验证 |
| OFFLINE 与真实 LLM 分表评测 | 真实 LLM 三例结果：非容量结论 |
| 当前是仓储补货项目 | ICD、医疗隐私、诊疗编码：当前不存在，仅能讨论扩展设计 |
